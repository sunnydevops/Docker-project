apiVersion: v1
kind: Pod
metadata:
  name:  client-pod
  labels:
    component: web
spec:
  hostNetwork: true
  containers:
    - name: client
      image: stephengrider/multi-client
      ports:
        - containerPort: 3000
      